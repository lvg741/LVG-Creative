{
  "name": "lvg-creative-static",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/index.html && cp styles.css dist/styles.css && cp -R public dist/public"
  }
}
